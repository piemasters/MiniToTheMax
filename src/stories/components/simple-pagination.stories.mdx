import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  Meta,
} from '@storybook/addon-docs/blocks';
import SimplePagination from '../../components/simple-pagination';

<Meta title="Components/SimplePagination" component={SimplePagination} />

# SimplePagination

With `MDX` we can define a story for `SimplePagination` right in the middle of our
markdown documentation.

<Preview>
  <Story name="default">
    <SimplePagination
      previous={{
        fields: {
          slug: 'previous',
        },
        frontmatter: {
          title: 'Previous Page',
        },
      }}
      next={{
        fields: {
          slug: 'next',
        },
        frontmatter: {
          title: 'Next Page',
        },
      }}
    />
  </Story>
</Preview>

# Props

<Props of={SimplePagination} />
