import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  Meta,
} from '@storybook/addon-docs/blocks';
import PageLink from '../../components/page-link';

<Meta title="Components/PageLink" component={PageLink} />

# PageLink

With `MDX` we can define a story for `PageLink` right in the middle of our
markdown documentation.

<Preview>
  <Story name="default">
    <PageLink to={'/'}>Test</PageLink>
  </Story>
</Preview>

# Props

<Props of={PageLink} />
