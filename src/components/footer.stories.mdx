import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  ColorPalette,
  ColorItem,
  Meta,
} from '@storybook/addon-docs/blocks';
import Footer from './footer';

<Meta title="MDX|Footer" component={Footer} />

# Footer

With `MDX` we can define a story for `Footer` right in the middle of our
markdown documentation.

<Preview>
  <Story name="default">
    <Footer />
  </Story>
</Preview>

# Props

<Props of={Footer} />
