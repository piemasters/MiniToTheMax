import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  Meta,
} from '@storybook/addon-docs/blocks';
import { SimplePagination } from './SimplePagination';

<Meta
  title="Components/SimplePagination"
  component={SimplePagination}
  parameters={{ jest: ['simple-pagination'] }}
  argTypes={{
    previous: {
      name: 'prevPage',
      description: 'Link to the previous page',
      defaultValue: { slug: 'previous', title: 'Previous Page' },
      control: { type: 'object' },
    },
    next: {
      name: 'nextPage',
      description: 'Link to the next page',
      defaultValue: { slug: 'next', title: 'Next Page' },
      control: { type: 'object' },
    },
  }}
/>

# SimplePagination

With `MDX` we can define a story for `SimplePagination` right in the middle of our
markdown documentation.

<Preview>
  <Story
    name="default"
    args={{
      previous: {
        slug: 'previous',
        title: 'Previous Page',
      },
      next: {
        slug: 'next',
        title: 'Next Page',
      },
    }}
  >
    {(args) => <SimplePagination {...args} />}
  </Story>
</Preview>

# Props

<Props story="default" />
